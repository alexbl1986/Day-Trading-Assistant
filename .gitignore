.venv
uv.lock
.env
__pycache__/
*.pyc
.pytest_cache/
.langgraph_api/
.vercel

# Eval outputs — keep the folder, ignore generated artifacts
artifacts/*
!artifacts/.gitkeep

# Private: discusses the real trader's workflow — never commit
docs/PROTOTYPE_GAPS.md
docs/PROTOTYPE_GAPS2.md
notes_to.self.md

# Deploying on railway and vercel instructions
deploying on railway and vercel.txt

# Local Claude Code permissions — machine-specific
.claude/settings.local.json


# all tests gitignored
tests/

docs/adr/

# data/book/ (formerly gitignored data/private/): anonymized book CSVs,
# committed since 2026-07-15 (identity rows removed, owner approved).
# Identified exports must never land in the repo.
